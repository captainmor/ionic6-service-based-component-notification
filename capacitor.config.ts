import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic6-service-based-component-notification',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
